<UserControl x:Class="LibraryInfoSystem.Components.GameComponent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LibraryInfoSystem.Components"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Margin" Value="10"/>
        </Style>
        
    </UserControl.Resources>
    
    <Grid Background="White" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Text="{Binding title, RelativeSource={RelativeSource AncestorType=UserControl}}" Grid.Column="0"/>
        <TextBlock Text="{Binding price, RelativeSource={RelativeSource AncestorType=UserControl}}" Grid.Column="1"/>
        <TextBlock Text="{Binding platform, RelativeSource={RelativeSource AncestorType=UserControl}}" Grid.Column="2"/>
        <Image x:Name="image_cover" Grid.Column="3" Width="50" Height="50"/>

    </Grid>
</UserControl>
